import unittest
from character.status import Status
from gamedata.parser_status import *

class ReactionTests(unittest.TestCase):
    def setUp(self):
        print("starting reaction tests")
    
    # def test_prompt_parse(self):
    #     prompt_string = '?[37m[41 H 24 M]'
    #     Prompt.parse(prompt_string)
    #     self.assertEqual()
        
